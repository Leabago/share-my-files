apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "share-my-files.fullname" . }}-test-connection"
  labels:
    {{- include "share-my-files.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "share-my-files.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
