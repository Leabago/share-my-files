apiVersion: v1
kind: Secret
metadata:
  name: {{ include "share-my-files.fullname" . }}
  labels:
    {{- include "share-my-files.labels" . | nindent 4 }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.crt | quote }}
  tls.key: {{ .Values.key | b64enc | quote }}